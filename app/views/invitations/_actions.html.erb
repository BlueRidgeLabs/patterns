<% invitation.aasm.events(permitted:true).each do |e| %>
            <%= button_to e.name,
                  event_research_session_invitations_path(@research_session,invitation,e.name),
                  method: :post,
                  remote: true,
                  class: "btn btn-sm btn-secondary dynamic-invitations m-1" %>
<% end %>
