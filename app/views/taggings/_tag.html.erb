<li id="tag-<%= tag.id %>">
  <% 
    params = [tag.id] 
    tag_name = raw "#{tag.name} <span class='badge badge-pill badge-secondary'>#{tag.taggings_count}</span>"
  %>
  <%= link_to tag_name, search_index_ransack_path("q[tags_id_eq_all]" => params) %>
</li>
